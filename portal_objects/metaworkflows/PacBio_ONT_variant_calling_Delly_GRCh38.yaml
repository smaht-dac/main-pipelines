## Pipeline information #####################################
#     General information for the pipeline
#############################################################
name: PacBio_ONT_variant_calling_Delly_GRCh38
description: Run Delly on long-read data (PacBio and ONT). |
             Produce structural variant calls in VCF format.  |
             Accept a single long-read CRAM file as input

category:
  - Variant Calling

## General arguments ########################################
#     Pipeline input, reference files, and general arguments
#       define all arguments for the pipeline here
#############################################################
input:

  # File inputs
  input_file_cram:
    argument_type: file.cram
    dimensionality: 0

  genome_reference_fasta:
    argument_type: file.fa
    files:
      - complete-reference-fasta-no-alt@GCA_000001405.15_GRCh38_no_decoy

  # Parameter inputs
  output_file_prefix:
    argument_type: parameter.string

## Workflows and dependencies ###############################
#     Information for the workflows and their dependencies
#############################################################
workflows:

  ## Workflow definition #####################
  #  delly_lr
  ############################################
  delly_lr:

    ## Specific arguments ##############
    ####################################
    input:

      # File arguments
      input_file_cram:
        argument_type: file.cram

      genome_reference_fasta:
        argument_type: file.fa

      # Parameter arguments
      output_file_prefix:
        argument_type: parameter.string

    ## Output ##############################
    #     Output files for the workflow
    ########################################
    output:

      # File output
      output_file_vcf_gz:
        description: Delly long-read output VCF
        data_category:
          - Somatic Variant Calls
        data_type:
          - SV
        output_status: Final Output
        # These fields are required to link metadata for the naming
        software:
          - smaht:Software-Delly_v.1.5.0
        reference_genome: GRCh38

    ## EC2 Configuration to use ########
    ####################################
    config:
      instance_type:
        - x2iedn.xlarge
        - x8aedz.xlarge
        - x2gd.2xlarge
        - x8g.2xlarge
      ebs_size: "2x"
      ebs_optimized: True
      spot_instance: True
      run_name: run_delly_long_read
      behavior_on_capacity_limit: wait_and_retry